#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "conexao.h"
#include "roteador.h"
#include "terminal.h"

int main(int argc, char* argv[]){

    return 0;
}

//funcao que le os parametros e separa-os para ler os arquivos no final
void(int i, char* c){
    FILE* arq = fopen("r", "teste.txt");
    if(arq == NULL){
        printf("Erro na leitura do arquivo de entrada!\n");
        return;
    }else{
    if(i > 1){
        //significa dizer que temos mais de um parametro digitado no terminal
    }
}
